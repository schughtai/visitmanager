import { PaymentAuthResponseDto } from './payment-auth-response.dto';

describe('PaymentAuthResponseDto', () => {
  it('should be defined', () => {
    expect(new PaymentAuthResponseDto()).toBeDefined();
  });
});
